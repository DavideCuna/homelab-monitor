# homelab-monitor
Lightweight system monitor for home servers and Raspberry Pi. Collects CPU, RAM, temperature, disk and network metrics via a Python collector, exposes them through a Flask REST API, and displays them on a simple dashboard. Runs entirely in Docker.
